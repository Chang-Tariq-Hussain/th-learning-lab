/**
 * Task 1 scope: layout only. `CellKind` is the only shared type this
 * stage needs — organelle data, positions, and descriptions are
 * intentionally deferred to a later task (see the component doc
 * comments in this folder for exactly where they'll plug in).
 */
export type CellKind = "animal" | "plant";
