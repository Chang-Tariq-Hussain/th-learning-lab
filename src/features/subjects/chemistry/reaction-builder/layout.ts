/** Shared SVG scene geometry for the Chemical Reaction Builder. Atom
 * radius matches Bond Builder / Molecule Builder (52) so atoms read as
 * the same "size" across every Chemistry visualization. */
export const SCENE_WIDTH = 900;
export const SCENE_HEIGHT = 400;
export const ATOM_RADIUS = 52;
